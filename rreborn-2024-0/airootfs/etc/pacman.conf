[options]

HoldPkg = pacman glibc

Architecture = auto

Color

Checkspace
VerbosePkgLists
ParallelDownloads = 5
ILoveCandy

SigLevel = Required DatabaseOptional
LocalFileSigLevel = Optional

[core]
Include = /etc/pacman.d/mirrorlist

[extra]
Include = /etc/pacman.d/mirrorlist

[community]
Include = /etc/pacman.d/mirrorlist

[multilib]
Include = /etc/pacman.d/mirrorlist

[rreborn-core-repo]
SigLevel = Optional Database Optional
